# Node dependencies
node_modules/
package-lock.json  # OPTIONAL — keep it if you want reproducible builds

# Hugo generated output
public/            # your static site output — it gets regenerated by `hugo`

# Hugo resources (processed images, css, etc)
resources/

# Hugo build lock file
.hugo_build.lock

# Go module cache (not always needed)
go.sum             # OPTIONAL — keep it if using Hugo modules so versions are locked

# Build logs / miscellaneous
hugo_stats.json

# IDE & system
.DS_Store
*.swp
*.swo
.idea/
.vscode/

# Platform-specific (if you want to avoid pushing deployment build artifacts)
.vercel/
.netlify/

# Optional: OS / other
Thumbs.db

